apiVersion: v1
kind: Service
metadata:
  name: classifier-service
  namespace: inference
  labels: { app: classifier-service }
spec:
  selector: { app: classifier-service }
  ports:
    - { name: http, port: 8001, targetPort: 8001 } 