apiVersion: v1
kind: Service
metadata:
  name: llm-service
  namespace: inference
spec:
  selector:
    app: llm-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
    - name: grpc
      port: 50051
      targetPort: 50051